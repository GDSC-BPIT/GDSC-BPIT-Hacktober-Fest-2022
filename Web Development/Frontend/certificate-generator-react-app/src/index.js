import { StrictMode } from "react";
import "./styles.css";
import App from "./App";
import ReactDOM from "react-dom";

const root = document.getElementById("root");

ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  root
);
